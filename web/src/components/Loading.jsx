export default function Loading() {
  return (
    <section className="min-h-screen w-full flex items-center justify-center" data-theme="calmpizza">
      <span
        className="loading loading-spinner loading-xl"
        aria-label="Loading"
      />
    </section>
  );
}